name: Run script

on:
  push:
    branches:
      - main

jobs:
  run-hi:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Run script
        run: bash ./script.sh
